# 2026-07-10

## 平台优化四项需求

1. **浏览器 title + 侧边栏名称**：title 改为「蜂云时代资产运营管理平台」，侧边栏 Logo 改为「运营管理平台」
2. **客户增加业务类型多选**：customer 表新增 `business_types` JSON 列（["算力租赁","算力服务","卫星数据"]），前后端均支持 CRUD，列表页彩色 tag 展示
3. **菜单重命名**：「算力租赁」→「算力租赁合同」
4. **算力服务合同四大类表单模板联动**：新增「模型调优服务」，四种大类（算力服务/算力优化服务/智能体服务/模型调优服务）选择时自动切换表单字段和规格模板。算力服务显示 vCPU/内存/存储，算力优化/智能体隐藏资源字段，模型调优服务隐藏周期（固定=1）

涉及的迁移文件：
- `007_add_customer_business_types.py`（customer 表新增 business_types）
- 构建部署时 Docker daemon 代理 192.168.180.251:7890 宕机，需临时移除代理配置才能 push 镜像
